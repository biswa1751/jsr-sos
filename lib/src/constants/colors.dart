import 'package:flutter/material.dart';

// handle all the colors for the app
class AppColors {
  static const blue = Color(0xFF027FFF);
  static const darkBlue = Color(0xFF17234D);
  static const white = Color(0xFFF6FAFB);
  static const lightGreen = Color(0xFF19E8E2);
  static const black = Colors.black;
  static const red = Color(0xFFDB0505);
  static const green = Color(0xFF05DB34);
  static const yellow = Color(0xFFF9FD32);
}
